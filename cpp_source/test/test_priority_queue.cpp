#include <bits/stdc++.h>
using namespace std;
int main(){
    // priority_queue<int> q;
    // q.push(3);
    // q.push(4);
    // q.push(1);
    // cout<<q.top()<<endl;q.pop();
    // cout<<q.top()<<endl;q.pop();
    // cout<<q.top()<<endl;q.pop();
    int a[3] = {1, 4, 2};
    sort(a, a+3);
    cout<<a[0];
    system("pause");
    return 0;
}